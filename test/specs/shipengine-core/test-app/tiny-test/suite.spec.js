// "use strict";

// const { expect } = require("chai");
// const Suite = require("../../../../../lib/shipengine-core/test-app/tiny-test/suite")
//   .default;

// const subject = new Suite({ title: "test subject", fn: () => {} });

// describe("Test", () => {
//   describe("testCache setter", () => {
//     it("overwrites the existing tests", () => {
//       expect(subject.toString()).to.be.a("string");
//       expect(subject.toString()).to.be.include(subject.title);
//       expect(subject.toString()).to.be.include(subject.truncatedSha());
//     });
//   });
// });
