{
  "name": "inventory-app-sample",
  "version": "1.0.1",
  "main": "./lib/index.js",
  "types": "./lib/index.ts",
  "scripts": {
    "start": "tsc && node ./lib/serve.js",
    "build": "tsc",
    "format": "prettier --single-quote --trailing-comma=all --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^2.3.0",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "@shipengine/connect-inventory-api": "file:../../packages/inventory-api",
    "@shipengine/connect-runtime": "file:../../packages/runtime"
  }
}
