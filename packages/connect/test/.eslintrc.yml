# ESLint config
# http://eslint.org/docs/user-guide/configuring

env:
  mocha: true

rules:
  "unicorn/no-abusive-eslint-disable": "warn"

ignorePatterns:
  - "specs/typescript.spec.ts"
  - "fixtures/**/*.js"
