import { RequestBase } from './request-base';

/** @description A request to verify if a user's order source is configured properly for delivery options */
export interface VerifyDeliveryOptionsRequest extends RequestBase {}
