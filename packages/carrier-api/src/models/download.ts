export interface Download {
  /**
   * The url where the created label can be downloaded.
   */
  href?: null | string;
  /**
   * The base64 encoded label image.
   */
  label_data?: null | string;
}
