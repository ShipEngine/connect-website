x-generator: NSwag v12.0.11.0 (NJsonSchema v9.13.13.0 (Newtonsoft.Json v12.0.0.0))
openapi: 3.0.0
info:
  title: ShipEngine Carrier API
  description: This API specification describes the canonical ShipEngine Carrier API connector module. A module which implements one or more endpoints from this specification can be integrated with the ShipEngine Carrier Platform.
  version: 1.14.5
consumes:
  - application/json-patch+json
  - application/json
  - text/json
  - application/*+json
servers:
  - url: 'http://localhost:3005'
    description: CarrierAPI
paths:
  /Track:
    post:
      tags:
        - Tracking
      operationId: Tracking_Track
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: trackingRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrackingRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/TrackingResponse'
  /GetRates:
    post:
      tags:
        - Rates
      operationId: Rates_GetRates
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: getRatesRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetRatesRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/GetRatesResponse'
        '400':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/BadRequestResponse'
        '401':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/UnauthorizedResponse'
        '404':
          description: ''
        '500':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/InternalServerErrorResponse'
        '520':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/ExternalServerErrorResponse'
  /SchedulePickup:
    post:
      tags:
        - Pickup
      operationId: Pickup_SchedulePickup
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: schedulePickupRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SchedulePickupRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/SchedulePickupResponse'
  /CancelPickup:
    post:
      tags:
        - Pickup
      operationId: Pickup_CancelPickup
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: cancelPickupRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelPickupRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/CancelPickupResponse'
  /CreateManifest:
    post:
      tags:
        - Manifests
      operationId: Manifests_CreateManifest
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: createManifestRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateManifestRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/CreateManifestResponse'
  /CreateLabel:
    post:
      tags:
        - Labels
      operationId: Labels_CreateLabel
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: createLabelRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLabelRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/CreateLabelResponse'
        '400':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/BadRequestResponse'
        '401':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/UnauthorizedResponse'
        '404':
          description: ''
        '500':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/InternalServerErrorResponse'
        '520':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/ExternalServerErrorResponse'
  /VoidLabels:
    post:
      tags:
        - Labels
      operationId: Labels_VoidLabels
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
            nullable: true
          x-position: 2
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 3
      requestBody:
        x-name: voidLabelsRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoidLabelsRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/VoidLabelsResponse'
        '400':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/BadRequestResponse'
        '401':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/UnauthorizedResponse'
        '404':
          description: ''
        '500':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/InternalServerErrorResponse'
        '520':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/ExternalServerErrorResponse'
  /Register:
    post:
      tags:
        - Accounts
      operationId: Accounts_Register
      parameters:
        - name: ShipStation-TransactionID
          in: header
          schema:
            type: string
            format: guid
          x-position: 2
      requestBody:
        x-name: registerRequest
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExampleRegisterRequest'
        required: true
        x-position: 1
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/ExampleRegisterResponse'
        '400':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/BadRequestResponse'
        '401':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/UnauthorizedResponse'
        '404':
          description: ''
        '500':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/InternalServerErrorResponse'
        '520':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
                oneOf:
                  - $ref: '#/components/schemas/ExternalServerErrorResponse'
components:
  schemas:
    TrackingResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: This model represents the successful response from a tracking request.
          additionalProperties: false
          properties:
            tracking_info:
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/TrackingInfo'
    TrackingInfo:
      type: object
      description: This model represents tracking information for a shipment.
      additionalProperties: false
      properties:
        carrier_name:
          type: string
          description: The human readable name of the carrier tracking this shipment.
          nullable: true
        tracking_number:
          type: string
          description: The carrier specific tracking identifier for this shipment.
          nullable: true
        standardized_status_code:
          description: This represents the ShipEngine supported status codes for a shipment.
          oneOf:
            - $ref: '#/components/schemas/StandardizedStatusCodes'
        carrier_status_code:
          type: string
          description: The carrier specific status code of this shipment's tracking status.
          nullable: true
        carrier_status_description:
          type: string
          description: The human readable description of this shipment's tracking status.
          nullable: true
        shipped_datetime:
          type: string
          description: 'When the package(s) in this shipment were turned over to the carrier. Formatted per https://tools.ietf.org/html/rfc3339. Must be in UTC.'
          format: date-time
          nullable: true
        estimated_delivery_datetime:
          type: string
          description: 'When the package(s) in this shipment are expected to arrive at their destination. Formatted per https://tools.ietf.org/html/rfc3339. Must be in UTC.'
          format: date-time
          nullable: true
        actual_delivery_datetime:
          type: string
          description: 'When the package(s) in this shipment arrived at their destination. Formatted per https://tools.ietf.org/html/rfc3339. Must be in UTC.'
          format: date-time
          nullable: true
        shipping_problem_description:
          type: string
          description: A human readable description of why the shipment is in an error state.
          nullable: true
        service:
          description: A shipping provider specific service.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Service'
        packaging:
          type: string
          description: The carrier observed count of packages in this shipment.
          nullable: true
        package_count:
          type: integer
          description: The carrier observed count of packages in this shipment.
          format: int32
          nullable: true
        events:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/TrackEvent'
        shipping_problem:
          type: boolean
          description: Whether or not this shipment has entered an error state.
        shipping_problem_code:
          type: string
          description: The unique identifying code for the type of error encountered by this shipment.
          nullable: true
        error_description:
          type: string
          description: The human readable explanation of the error encountered by this shipment.
          nullable: true
    StandardizedStatusCodes:
      type: string
      description: |-
        This represents the ShipEngine supported status codes for a shipment.
        UN - Unknown
        AC - Accepted
        IT - In Transit
        DE - Delivered
        EX - Exception
        AT - Delivery Attempted
        NY - Not Yet In System
      x-enumNames:
        - Unknown
        - Accepted
        - InTransit
        - Delivered
        - Exception
        - DeliveryAttempt
        - NotYetInSystem
      enum:
        - UN
        - AC
        - IT
        - DE
        - EX
        - AT
        - NY
    Dimensions:
      type: object
      description: This model represents the measurements for dimensions.
      additionalProperties: false
      properties:
        length:
          type: number
          description: The length of the package.
          format: decimal
          nullable: true
        width:
          type: number
          description: The width of the package.
          format: decimal
          nullable: true
        height:
          type: number
          description: The height of the package.
          format: decimal
          nullable: true
    Service:
      type: object
      description: A shipping provider specific service.
      additionalProperties: false
      properties:
        code:
          type: string
          description: The code specific to a shipping provider service.
          nullable: true
        name:
          type: string
          description: The name of this shipping provider service.
          nullable: true
    TrackEvent:
      type: object
      description: This model represents a tracking event for a shipment.
      additionalProperties: false
      properties:
        event_datetime:
          type: string
          description: 'When this tracking event occurred. Formatted per https://tools.ietf.org/html/rfc3339. Must be in UTC.'
          format: date-time
        event_datetime_local:
          type: string
          description: 'Most providers do not provide tracking events in UTC, and without time zone or geo information its hard to deduce.'
          format: date-time
          nullable: true
        event_code:
          type: string
          description: Carrier's specific event code
          nullable: true
        description:
          type: string
          description: Description of the package status at this event
          nullable: true
        city:
          type: string
          description: City or Locality for this event
          nullable: true
        state:
          type: string
          description: State or Province for this event
          nullable: true
        postal_code:
          type: string
          description: Postal Code for this event
          nullable: true
        country:
          type: string
          description: Country for this event
          nullable: true
        company:
          type: string
          nullable: true
        signer:
          type: string
          description: 'If the event had a signer associated, normally on delivery'
          nullable: true
        geo:
          description: Coordinates for the event
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/LatLong'
    LatLong:
      type: object
      description: This model represents a geographic point.
      additionalProperties: false
      properties:
        lat:
          type: number
          description: 'The latitude of the point. Represented as signed degrees. http://www.geomidpoint.com/latlon.html'
          format: decimal
        long:
          type: number
          description: 'The longitude of the point. Represented as signed degrees http://www.geomidpoint.com/latlon.html'
          format: decimal
    BaseResponse:
      type: object
      additionalProperties: false
      properties:
        metadata:
          type: object
          description: |-
            This is an optional schemaless object that you may return with a successful response. Anything returned
            under this key will be included in all future requests. For example, you may store additional static
            properties about the end user or their connection to the carrier. The maximum storage size
            for data under this key is 4KB.
          nullable: true
          additionalProperties: {}
    TrackingRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          description: This model represents the payload that is sent to request tracking information.
          additionalProperties: false
          properties:
            tracking_number:
              type: string
              nullable: true
            is_return:
              type: boolean
              description: Whether this represents a return shipment. If the field is absent it should be interpreted as false
            identifiers:
              type: array
              description: This array represents the custom identifiers for a carrier.
              nullable: true
              items:
                $ref: '#/components/schemas/TrackingIdentifier'
            attributes:
              type: array
              description: Extra attributes of a shipment needed in tracking
              nullable: true
              items:
                $ref: '#/components/schemas/TrackingAttribute'
    TrackingIdentifier:
      type: object
      description: This model represents a key value pair for identifying a shipment for tracking.
      additionalProperties: false
      properties:
        type:
          type: string
          description: 'The tracking identifier type, either `tracking_number` or `carrier_transaction_id`.'
          nullable: true
        value:
          type: string
          description: The value associated with that key.
          nullable: true
    TrackingAttribute:
      type: object
      description: This model represents attributes from the shipping provider about a shipment necessary for tracking.
      additionalProperties: false
      properties:
        type:
          type: string
          nullable: true
        value:
          type: string
          nullable: true
    BaseRequest:
      type: object
      additionalProperties: false
      properties:
        transaction_id:
          type: string
          description: The transaction ID uniquely represents this request. If the request is retried then this transaction ID will be the same. You should only perform the requested action once per given transaction ID.
          format: guid
        metadata:
          type: object
          nullable: true
          additionalProperties: {}
    BadRequestResponse:
      allOf:
        - $ref: '#/components/schemas/ErrorResponse'
        - type: object
          description: This model represents a response where there was a client error.
          additionalProperties: false
          properties:
            errors:
              type: array
              description: A list of errors
              nullable: true
              items:
                type: string
    ErrorResponse:
      type: object
      additionalProperties: false
      properties:
        detailed_errors:
          type: array
          description: |-
            A list of detailed errors that occurred for this request.
            Remove all personally identifiable information.
          nullable: true
          items:
            $ref: '#/components/schemas/DetailedError'
    DetailedError:
      type: object
      description: This model represented a detailed error
      additionalProperties: false
      properties:
        standardized_error_code:
          description: The ShipEngine standardized error code associated with this error.
          oneOf:
            - $ref: '#/components/schemas/StandardizedErrorCode'
        external_error_code:
          type: string
          description: This is a shipping provider specific error code.
          nullable: true
        message:
          type: string
          description: A user friendly message about this error.
          nullable: true
        external_http_status_code:
          type: integer
          description: The HTTP status code returned from the external service provider.
          format: int32
          nullable: true
        raw_external_context:
          type: string
          description: The serialized response containing the error from an external service provider.
          nullable: true
    StandardizedErrorCode:
      type: string
      description: A list of standard error identifiers for errors the module can return
      x-enumNames:
        - Generic
        - Serialization
        - UnhandledModuleException
        - Validation
        - ExternalBadRequest
        - ExternalUnauthorized
        - ExternalUnprocessableEntity
        - ExternalTooManyRequests
        - ExternalNotImplemented
        - ExternalBadGateway
        - ExternalServiceUnavailable
        - ExternalGatewayTimeout
        - ExternalServerError
      enum:
        - generic
        - serialization
        - unhandled_module_exception
        - validation
        - external_bad_request
        - external_unauthorized
        - external_unprocessable_entity
        - external_too_many_requests
        - external_not_implemented
        - external_bad_gateway
        - external_service_unavailable
        - external_gateway_timeout
        - external_server_error
    UnauthorizedResponse:
      allOf:
        - $ref: '#/components/schemas/ErrorResponse'
        - type: object
          description: This model represents a response that the request was unauthorized to perform some action.
          additionalProperties: false
    InternalServerErrorResponse:
      allOf:
        - $ref: '#/components/schemas/ErrorResponse'
        - type: object
          description: This model represents a module level error that was caught.
          additionalProperties: false
    ExternalServerErrorResponse:
      allOf:
        - $ref: '#/components/schemas/ErrorResponse'
        - type: object
          description: This model represents a server error with the external services the module depends on.
          additionalProperties: false
    GetRatesResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: This model represents the response for a get rates request.
          additionalProperties: false
          properties:
            rates:
              type: array
              description: |-
                A list of rates for shipping services. If you use service codes there may be exactly one rate returned for
                the requested service. If you use service codes there may be more than one rate to provide additional
                options. If you don't use service codes there may be one or more rates. If you don't provide real time
                rates you should return a single hard coded zero value rate.
              nullable: true
              items:
                $ref: '#/components/schemas/Rate'
    Rate:
      type: object
      description: This model represents a rate for a given service code.
      additionalProperties: false
      properties:
        service_code:
          type: string
          description: |-
            The service code uniquely identifies a shipping service that you offer. Which service codes can be passed
            to you will be configured in ShipEngine.
            If this field is missing, the rate will be discarded.
          nullable: true
        ship_datetime:
          type: string
          description: |-
            When the package should ship for this rate to be valid. ** This differs from previous documentation
            Formatted per the https://tools.ietf.org/html/rfc3339 spec. Must be in UTC.
          format: date-time
          nullable: true
        estimated_delivery_datetime:
          type: string
          description: |-
            When the package(s) in this shipment are expected to arrive at their destination.
            Formatted per https://tools.ietf.org/html/rfc3339. Must be in UTC.
          format: date-time
          nullable: true
        billing_line_items:
          type: array
          description: Individual Billing Line items for the rate.
          nullable: true
          items:
            $ref: '#/components/schemas/BillingLineItem'
        shipping_amount:
          description: The total cost to ship the package(s) in this request.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Currency'
        error_messages:
          type: array
          description: A list of discrete errors.
          nullable: true
          items:
            type: string
        warning_messages:
          type: array
          description: |-
            A list of discrete warnings. This should be data the end user needs to be aware of.
            These should not stop the request from succeeding, or a label being purchased with this rate.
          nullable: true
          items:
            type: string
        negotiated_rate:
          type: boolean
          description: Whether these are negotiated rates.
    BillingLineItem:
      type: object
      additionalProperties: false
      properties:
        billing_category:
          $ref: '#/components/schemas/BillingCategories'
        carrier_description:
          type: string
          description: If the carrier provides a description from their API about the billing charge (not specific to the user). Maximum length of 100 characters.
          nullable: true
        carrier_billing_code:
          type: string
          description: If the carrier provides a billing code for
          nullable: true
        memo:
          type: string
          description: 'Maximum length of 250 characters. This will often be empty; however, if there was additionally specific information about the charge it should go here, could also be a reference number. Maximum length of 250 characters.'
          nullable: true
        amount:
          description: Total amount of the line item
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Currency'
    BillingCategories:
      type: string
      description: ''
      x-enumNames:
        - Uncategorized
        - Shipping
        - Insurance
        - Confirm
        - Discount
        - FuelCharge
        - AdditionalFees
        - Tariff
        - Tax
        - Delivery
        - Handling
        - SpecialGoods
        - Pickup
        - LocationFee
        - Oversize
        - Returns
        - Notifications
      enum:
        - uncategorized
        - shipping
        - insurance
        - confirm
        - discount
        - fuel_charge
        - additional_fees
        - tariff
        - tax
        - delivery
        - handling
        - special_goods
        - pickup
        - location_fee
        - oversize
        - returns
        - notifications
    Currency:
      type: object
      required:
        - amount
      properties:
        currency:
          type: string
        amount:
          type: string
    GetRatesRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          description: This model represents the payload sent to get a rate for a shipment.
          additionalProperties: false
          properties:
            service_code:
              type: string
              description: |-
                If present, a specific service is being requested, and that is the only rate required to fulfill the request.
                The service code uniquely identifies a shipping service that you offer.
                Which service codes can be passed to you will be configured in ShipEngine.
              nullable: true
            ship_datetime:
              type: string
              description: |-
                When the package is expected to ship. Not guaranteed to be in the future. Formatted per the
                https://tools.ietf.org/html/rfc3339 spec. Will always be in UTC.
              format: date-time
            confirmation:
              description: The expected delivery confirmation. If the field is absent it should be interpreted as "None".
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ConfirmationTypes'
            advanced_options:
              description: |-
                This is a schemaless object. It is for open ended customizations unique to particular carriers. The
                documented keys are some common options shared by many carriers, but are not definitive. Advanced options
                you support will be defined in ShipEngine. If the field is absent it should be interpreted as the default
                value for any applicable options, e.g. false for booleans.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/AdvancedOptions'
            insurance_provider:
              description: |-
                The insurance provider for the insured value of the label, `carrier` indicates that the user is requesting
                insurance from the carrier directly, anything else is extra information that should not result
                in a transaction.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/InsuranceProviders'
            is_return_label:
              type: boolean
              description: |-
                Whether this label is to return a previously shipped package. If the field is absent it should be
                interpreted as false.
            is_residential:
              type: boolean
              description: |-
                Whether this package ships from a residential address. If the field is absent it should be
                interpreted as false.
            packages:
              type: array
              description: All the packages that make up this shipment. There will always be at least one package defined.
              nullable: true
              items:
                $ref: '#/components/schemas/Package'
            customs:
              description: This model represents the customs declarations of a shipment.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/Customs'
            ship_to:
              description: The shipment recipient's address. It may or may not have been validated.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipTo'
            ship_from:
              description: The shipment sender's address. It may or may not have been validated.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipFrom'
            pickup_location:
              description: |-
                If this is a pickup shipment, a pickup location will be populated for where the recipient can
                pickup the shipment.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PudoLocation'
            international:
              type: boolean
              description: Whether this shipment is deemed international.
              nullable: true
            next_day:
              type: boolean
              description: |-
                Whether this shipment is expected to use a next day service class. If the field is absent it
                should be interpreted as false
              nullable: true
            fulfillment_plan_details:
              description: This model contains information about the fulfillment plan. This is not provided for all rate requests.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/FulfillmentPlanDetails'
    ConfirmationTypes:
      type: string
      description: ''
      x-enumNames:
        - None
        - Delivery
        - Signature
        - AdultSignature
        - DirectSignature
      enum:
        - None
        - Delivery
        - Signature
        - AdultSignature
        - DirectSignature
    AdvancedOptions:
      type: object
      description: |-
        This is a schemaless object. It is for open ended customizations unique to particular carriers. The documented
        keys are some common options shared by many carriers, but are not definitive. Advanced options you support will
        be defined in ShipEngine. If the field is absent it should be interpreted as the default value for any
        applicable options, e.g. false for booleans.
      additionalProperties:
        nullable: true
      properties:
        contains_alcohol:
          type: boolean
          description: Describes whether or not the shipment contains alcohol.
        no_postage:
          type: boolean
          description: Describes whether no postage is necessary
        nonmachineable:
          type: boolean
          description: Describes whether or not the shipment is non automatically sortable with machines.
        bill_duties_to_sender:
          type: boolean
          description: Describes whether or not customs duties should be billed to the sender.
        saturday_delivery:
          type: boolean
          description: Describes whether or not the shipment is requested to be delivered on a Saturday.
    InsuranceProviders:
      type: string
      description: ''
      x-enumNames:
        - None
        - ShipStation
        - Carrier
        - External
      enum:
        - None
        - ShipStation
        - Carrier
        - External
    Package:
      type: object
      description: This model represents an individual package that is part of a shipment.
      additionalProperties: false
      properties:
        package_code:
          type: string
          description: Package code defined for the carrier in ShipEngine.
          nullable: true
        dimension_details:
          description: 'This model represents the dimensions for a package represented in its original unit, inches, and centimeters.'
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/DimensionDetails'
        weight_details:
          description: 'This model represents the weight of a package in its original unit, ounces, and grams.'
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/WeightDetails'
        insured_value:
          description: The insured value of this package.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Currency'
        label_messages:
          description: |-
            Customized strings by the user that are expected to appear on the label, but can also be sent as references
            to the carrier to appear on invoices. Most carriers have the ability to add a reference number.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/LabelMessage'
        weight:
          type: number
          description: This property has been deprecated. Please use weight_details.
          format: decimal
          nullable: true
        dimensions:
          description: This property has been deprecated. Please use dimension_details.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Dimensions'
        customs:
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Customs'
    DimensionDetails:
      type: object
      description: 'This model represents the dimensions for a package represented in its original unit, inches, and centimeters.'
      additionalProperties: false
      properties:
        dimensions_in_centimeters:
          description: The dimensions in Centimeters rounded to the fourth decimal place.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Dimensions'
        dimensions_in_inches:
          description: The dimensions in Inches rounded to the fourth decimal place.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Dimensions'
        source_dimensions:
          description: The dimensions in the original dimensions rounded to the fourth decimal place.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Dimensions'
        source_dimension_unit:
          description: This represents the original unit of measurement.
          oneOf:
            - $ref: '#/components/schemas/DimensionUnit'
    DimensionUnit:
      type: string
      description: This model represents the supported units for dimensions.
      x-enumNames:
        - Inches
        - Centimeters
      enum:
        - inches
        - centimeters
    WeightDetails:
      type: object
      description: 'This model represents the weight of a package in its original unit, ounces, and grams.'
      additionalProperties: false
      properties:
        weight_in_ounces:
          type: number
          description: The weight in ounces rounded to the fourth decimal place.
          format: decimal
        weight_in_grams:
          type: number
          description: The weight in grams rounded to the fourth decimal place.
          format: decimal
        source_weight:
          type: number
          description: The weight in its original unit of measurement rounded to the fourth decimal place.
          format: decimal
        source_weight_unit:
          description: This model describes the possible units of weight.
          oneOf:
            - $ref: '#/components/schemas/WeightUnit'
    WeightUnit:
      type: string
      description: This model describes the possible units of weight.
      x-enumNames:
        - Grams
        - Ounces
        - Kilograms
        - Pounds
      enum:
        - grams
        - ounces
        - kilograms
        - pounds
    LabelMessage:
      type: object
      additionalProperties: false
      properties:
        reference1:
          type: string
          nullable: true
        reference2:
          type: string
          nullable: true
        reference3:
          type: string
          nullable: true
    Customs:
      type: object
      additionalProperties: false
      properties:
        contents:
          description: |-
            Allowed values are "documents", "gift", "merchandise", "returned_goods", or "sample". If a match isn't
            found, this should default to Merchandise.
          oneOf:
            - $ref: '#/components/schemas/CustomsContentTypes'
        non_delivery:
          description: |-
            What to do if the shipment is undeliverable. If the field is absent the undeliverable behavior is
            unspecified and should be defaulted per carrier.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/CustomsNonDelivery'
        customs_items:
          type: array
          description: List of customs items to declare
          nullable: true
          items:
            $ref: '#/components/schemas/CustomsItem'
        buyer_shipping_amount_paid:
          description: |-
            Indicates how much the buyer paid for shipping, if any. This amount may be different than the shipping cost billed
            to the shipper.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Currency'
        duties_paid:
          description: |-
            The amount of duties paid by the shipper, if any. This is generally only necessary for DDP
            (bill_duties_to_sender) shipments.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Currency'
    CustomsContentTypes:
      type: string
      description: ''
      x-enumNames:
        - Merchandise
        - Documents
        - Gift
        - Sample
        - ReturnedGoods
      enum:
        - merchandise
        - documents
        - gift
        - sample
        - returned_goods
    CustomsNonDelivery:
      type: string
      description: What to do if the shipment is undeliverable.
      x-enumNames:
        - ReturnToSender
        - TreatAsAbandoned
      enum:
        - return_to_sender
        - treat_as_abandoned
    CustomsItem:
      type: object
      description: This model represents the customs declaration for a single item.
      additionalProperties: false
      properties:
        description:
          type: string
          description: |-
            A user specified free form string describing this customs item. If the field is absent the user has
            not specified a description.
          nullable: true
        quantity:
          type: integer
          description: The user specified count of items in this declaration. This value should be 1 or greater
          format: int32
        value:
          description: |-
            The user specified declared customs value of this customs item. Generally, this value needs to be greater
            than 0 to be accepted through customs.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Currency'
        country_of_origin:
          type: string
          description: The user specified declared country of origin of this customs item.
          nullable: true
        harmonized_tariff_code:
          type: string
          description: 'The user specified Harmonized Tariff Code. See https://hts.usitc.gov/ for more information.'
          nullable: true
        sku:
          type: string
          description: The user specified SKU of this customs item. This field is completely free form.
          nullable: true
        sku_description:
          type: string
          description: The user specified SKU description of this customs item. This field is completely free form.
          nullable: true
        item_weight:
          description: The weight of a single item on this customs line.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/WeightDetails'
    ShipTo:
      allOf:
        - $ref: '#/components/schemas/AddressBase'
        - type: object
          additionalProperties: false
          properties:
            tax_identifiers:
              type: array
              description: Tax IDs associated with the consignee.
              nullable: true
              items:
                $ref: '#/components/schemas/TaxIdentifier'
    TaxIdentifier:
      type: object
      description: 'Entity representing a Tax Identification number, type, and country of registration.'
      additionalProperties: false
      properties:
        id:
          type: string
          description: Identification number.
          nullable: true
        type:
          description: The Tax ID type.
          oneOf:
            - $ref: '#/components/schemas/TaxIdentificationType'
        registration_country:
          type: string
          description: The country where the Tax ID is registered with.
          nullable: true
        description:
          type: string
          description: Description of the tax ID that may give the customs agent more context.
          nullable: true
    TaxIdentificationType:
      type: string
      description: Specifies the supported tax identification types.
      x-enumNames:
        - TIN
        - EIN
        - SSN
        - VAT
        - EORI
        - IOSS
        - PAN
        - VOEC
      enum:
        - tin
        - ein
        - ssn
        - vat
        - eori
        - ioss
        - pan
        - voec
    AddressBase:
      type: object
      x-abstract: true
      additionalProperties: false
      properties:
        name:
          type: string
          description: The full name of the contact contact associated with the address
          nullable: true
        first_name:
          type: string
          description: The first name of the contact associated with the address
          nullable: true
        last_name:
          type: string
          description: The last name of the contact associated with the address
          nullable: true
        email:
          type: string
          description: The email of the contact associated with the address
          nullable: true
        phone_number:
          type: string
          description: The phone number of the contact associated with the address
          nullable: true
        company_name:
          type: string
          description: The name of the company associated with this address.
          nullable: true
        address_lines:
          type: array
          description: The address lines of the address.
          nullable: true
          items:
            type: string
        city_locality:
          type: string
          description: 'The city or locality of the address, where applicable.'
          nullable: true
        state_province:
          type: string
          description: 'The state or province of the address, where applicable.'
          nullable: true
        postal_code:
          type: string
          description: The postal code of the address.
          nullable: true
        country_code:
          type: string
          description: The two character country code of the shipment address. The codes are specified by ISO 3166-1 alpha-2.
          nullable: true
        address_residential_indicator:
          description: Whether the shipment address is residential.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/AddressResidentialIndicator'
        address_metadata:
          type: object
          description: Additional metadata provided for specific requests.
          nullable: true
          additionalProperties:
            type: string
    AddressResidentialIndicator:
      type: string
      description: ''
      x-enumNames:
        - Unknown
        - 'Yes'
        - 'No'
      enum:
        - unknown
        - 'yes'
        - 'no'
    ShipFrom:
      allOf:
        - $ref: '#/components/schemas/AddressBase'
        - type: object
          description: The shipment sender's address. It may or may not have been validated.
          additionalProperties: false
          properties:
            tax_identifiers:
              type: array
              description: Tax IDs associated with the exporter.
              nullable: true
              items:
                $ref: '#/components/schemas/TaxIdentifier'
    PudoLocation:
      allOf:
        - $ref: '#/components/schemas/AddressBase'
        - type: object
          description: Pickup/Dropoff Location used by carriers that offer this service.
          additionalProperties: false
          properties:
            carrier_code:
              type: string
              description: Standardized carrier code the location ID belongs to.
              nullable: true
            location_id:
              type: string
              description: Location ID necessary to lookup the location.
              nullable: true
    FulfillmentPlanDetails:
      type: object
      description: This model contains information about the fulfillment plan.
      additionalProperties: false
      properties:
        external_sales_order_identifiers:
          type: array
          description: A list of identifiers for the sales order.
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        items:
          type: array
          description: Information about the fulfillment plan item.
          nullable: true
          items:
            $ref: '#/components/schemas/FulfillmentPlanItems'
        raw_external_source:
          description: This model represents a raw external source of information.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/RawExternalSource'
    Identifier:
      type: object
      description: 'A key value pair, allowing custom properties to be stored.'
      additionalProperties: false
      properties:
        type:
          type: string
          description: The key.
          nullable: true
        value:
          type: string
          description: The value.
          nullable: true
    FulfillmentPlanItems:
      type: object
      description: This contains information about the fulfillment plan for a set of items.
      additionalProperties: false
      properties:
        external_sales_order_identifiers:
          type: array
          description: A list of identifiers for the sales order.
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        external_product_identifiers:
          type: array
          description: A list of identifiers specific to the selling channel.
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        name:
          type: string
          description: The name of the item.
          nullable: true
        quantity:
          type: integer
          description: The number of items.
          format: int32
    RawExternalSource:
      type: object
      description: This model represents a raw external source of information.
      additionalProperties: false
      properties:
        marketplace_code:
          type: string
          description: Canonical representation of the fulfillment plan's and/or sales order's original source
          nullable: true
    SchedulePickupResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: The response for scheduling a pickup.
          additionalProperties: false
          properties:
            confirmation:
              description: The confirmation code associated with the successful creation of a pickup
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupConfirmation'
            pickup_windows:
              type: array
              description: A list of dates and times when the carrier intends to be available to pickup
              nullable: true
              items:
                $ref: '#/components/schemas/PickupWindow'
            remarks:
              type: string
              description: A custom message returned from the carrier
              nullable: true
            custom_properties:
              type: object
              description: A grab bag of custom properties that will be persisted and sent back with the cancel pickup request
              nullable: true
              additionalProperties:
                type: string
            billing_line_items:
              type: array
              description: Individual Billing Line items for the rate.
              nullable: true
              items:
                $ref: '#/components/schemas/BillingLineItem'
    PickupConfirmation:
      type: object
      description: This model contains the confirmation information for a scheduled pickup.
      additionalProperties: false
      properties:
        confirmation_id:
          type: string
          description: The primary confirmation ID for this request.
          nullable: true
        alternate_identifiers:
          type: array
          description: Other identifiers potentially needed in other calls.
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        shipment_identifiers:
          type: array
          description: List of the shipments added for this pickup.
          nullable: true
          items:
            $ref: '#/components/schemas/ShipmentIdentifier'
    ShipmentIdentifier:
      type: object
      description: This model represents identifying information about a shipment.
      additionalProperties: false
      properties:
        tracking_number:
          type: string
          description: The tracking number associated with the shipment
          nullable: true
        alternate_identifiers:
          type: array
          description: A list of alternate identifiers that can be used to identify a shipment
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
    PickupWindow:
      type: object
      description: This model represents a window of time a pickup could be made.
      additionalProperties: false
      properties:
        time_zone_iana:
          type: string
          description: 'The local time zone represented in IANA. See https://www.iana.org/time-zones'
          nullable: true
        pickup_date:
          type: string
          description: 'The local date the pickup will occur. Formatted per https://tools.ietf.org/html/rfc3339'
          format: date-time
        start_time:
          type: string
          description: 'The time relative to the time_zone_iana that the carrier says that the pickup could occur. Formatted per https://tools.ietf.org/html/rfc3339'
          format: date-time
          nullable: true
        end_time:
          type: string
          description: 'The end of the window when the pickup could be made relative to the time_zone_iana. Formatted per https://tools.ietf.org/html/rfc3339'
          format: date-time
          nullable: true
    SchedulePickupRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          description: A request to schedule a pickup with a carrier
          additionalProperties: false
          properties:
            location:
              description: |-
                Where it's being picked up.
                Contact information for the pickup, like an on-duty manager that would be able to oversee
                the pickup at the location.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupLocationDetails'
            contact:
              description: Contact information about the person there to meet the driver.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupContactDetails'
            pickup_details:
              description: What is being picked up.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupShipmentDetails'
            requested_pickup_window:
              description: The time window that the user is requesting for the pickup to be made
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupWindow'
    PickupLocationDetails:
      type: object
      description: This model represents the place where the pickup should occur
      additionalProperties: false
      properties:
        pickup_address:
          description: The physical address of the warehouse or pickup address.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/ShipFrom'
        location_notes:
          type: string
          description: Human readable location information for the driver to find the location
          nullable: true
        pickup_options:
          type: object
          description: Custom options that are used by the carrier to determine pickup locations
          nullable: true
          additionalProperties:
            type: string
    PickupContactDetails:
      type: object
      description: Contact information for the person who will be present for the pickup.
      additionalProperties: false
      properties:
        first_name:
          type: string
          description: The first name of the person who will be there for the pickup.
          nullable: true
        last_name:
          type: string
          description: The last name of the person who will be there for the pickup.
          nullable: true
        email:
          type: string
          description: The e-mail address of the person who will be there for the pickup.
          nullable: true
        phone_number:
          type: string
          description: The phone number of the person who will be there for the pickup.
          nullable: true
        phone_number_extension:
          type: string
          description: The phone extension of the person who will be there for the pickup.
          nullable: true
    PickupShipmentDetails:
      type: object
      description: Contains information regarding the service code for the pickup request and the packages that will be picked up
      additionalProperties: false
      properties:
        pickup_service_code:
          type: string
          description: 'Service category for the carrier (ground, express, next_day)'
          nullable: true
        shipments:
          type: array
          description: The shipments that will be picked up
          nullable: true
          items:
            $ref: '#/components/schemas/ShippedShipment'
    ShippedShipment:
      type: object
      description: |-
        This model represents a shipment that has already been shipped, and contains
        summary information about the shipment.
      additionalProperties: false
      properties:
        tracking_number:
          type: string
          description: The carrier tracking number for this shipment
          nullable: true
        ship_to:
          description: The shipment recipient's address. It may or may not have been validated.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/ShipTo'
        alternative_identifiers:
          type: array
          description: Alternative identifiers associated with this package
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        service_code:
          type: string
          description: The service code used for this shipment
          nullable: true
        packages:
          type: array
          description: The list of packages associated with this shipment
          nullable: true
          items:
            $ref: '#/components/schemas/ShippedPackage'
        advanced_options:
          description: |-
            This is a schemaless object. It is for open ended customizations unique to particular carriers. The
            documented keys are some common options shared by many carriers, but are not definitive. Advanced options
            you support will be defined in ShipEngine. If the field is absent it should be interpreted as the default
            value for any applicable options, e.g. false for booleans.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/AdvancedOptions'
    ShippedPackage:
      type: object
      description: This is the model for a package that has had a label generated for it
      additionalProperties: false
      properties:
        tracking_number:
          type: string
          description: The carrier tracking number associated with this package
          nullable: true
        alternative_identifiers:
          type: array
          description: Alternative identifiers associated with this package
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        dimension_details:
          description: 'This model represents the dimensions for a package represented in its original unit, inches, and centimeters.'
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/DimensionDetails'
        weight_details:
          description: 'This model represents the weight of a package in its original unit, ounces, and grams.'
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/WeightDetails'
        package_code:
          type: string
          description: The code associated with the package type
          nullable: true
        weight:
          type: number
          description: The weight in grams
          format: decimal
          nullable: true
        dimensions:
          description: The dimensions for the package
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Dimensions'
    CancelPickupResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: This model represents a response for a successful cancel pickup request.
          additionalProperties: false
          properties:
            confirmation_id:
              type: string
              description: The confirmation id from the carrier associated with the cancellation request
              nullable: true
            successful:
              type: boolean
              description: A flag for whether or not the cancellation was successful
            status:
              type: string
              description: The optional status returned by the carrier
              nullable: true
            custom_properties:
              type: object
              description: A grab bag used to store additional information that might be useful for logging or internal reporting.
              nullable: true
              additionalProperties:
                type: string
    CancelPickupRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          description: A request to cancel a previously scheduled pickup
          additionalProperties: false
          properties:
            confirmation:
              description: The confirmation information provided by the SchedulePickup response
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupConfirmation'
            location:
              description: Where was it going to be picked up
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupLocationDetails'
            cancellation_details:
              description: Why the pickup is being cancelled
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/CancellationDetails'
            contact:
              description: Contact information about the person there to meet the driver.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupContactDetails'
            pickup_details:
              description: What was scheduled to be picked up
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PickupShipmentDetails'
            pickup_windows:
              type: array
              description: The time windows that the carrier returned in the Schedule Pickup Response
              nullable: true
              items:
                $ref: '#/components/schemas/PickupWindow'
            custom_properties:
              type: object
              description: The grab bag of properties necessary to cancel the pickup
              nullable: true
              additionalProperties:
                type: string
    CancellationDetails:
      type: object
      description: This model contains the explanation of why a user is cancelling a pickup.
      additionalProperties: false
      properties:
        reason:
          description: The reason the customer is cancelling their pickup request
          oneOf:
            - $ref: '#/components/schemas/CancellationReason'
        cancellation_options:
          type: object
          description: Additional properties about the cancellation
          nullable: true
          additionalProperties:
            type: string
        remarks:
          type: string
          description: Remarks about why the customer is cancelling the pickup
          nullable: true
    CancellationReason:
      type: string
      description: An enumeration of common reasons cancellations occur
      x-enumNames:
        - Other
        - NotReady
        - CostTooHigh
        - ServiceTooSlow
        - CarrierFailedPickup
      enum:
        - other
        - not_ready
        - cost_too_high
        - service_too_slow
        - carrier_failed_pickup
    CreateManifestResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: This model represents the successful response for a create manifest request.
          additionalProperties: false
          properties:
            transaction_id:
              type: string
              format: guid
            manifests:
              type: array
              description: 'The manifests created from this request. It should contain either a href or data, but not both.'
              nullable: true
              items:
                $ref: '#/components/schemas/Manifest'
    Manifest:
      type: object
      description: This model represents a single manifest created with a shipping provider.
      additionalProperties: false
      properties:
        manifest_id:
          type: string
          description: The unique identifier for this manifest.
          nullable: true
        document_download:
          description: The resource for where the manifest documentation can be downloaded.
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/ResourceDownload'
        document_href:
          type: string
          description: The public url where the manifest documentation can be downloaded.
          nullable: true
        document_data:
          type: string
          description: The base64 encoded manifest PDF.
          nullable: true
    ResourceDownload:
      type: object
      description: 'Used for generic downloads, containing the resource itself or a link to where it can be found'
      additionalProperties: false
      properties:
        href:
          type: string
          description: The public url where the created resource can be downloaded.
          nullable: true
        data:
          type: string
          description: The base64 encoded resource data.
          nullable: true
    CreateManifestRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          description: This model represents the request payload needed to manifest with a shipping provider.
          additionalProperties: false
          properties:
            ship_from:
              description: The shipment sender's address. It may or may not have been validated.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipFrom'
            included_labels:
              type: array
              description: The shipments to include in the manifest. There should be at least one shipment included in a manifest.
              nullable: true
              items:
                $ref: '#/components/schemas/ShippedShipment'
            excluded_labels:
              type: array
              description: The shipments to exclude from the manifest.
              nullable: true
              items:
                $ref: '#/components/schemas/ShippedShipment'
            open_datetime:
              type: string
              description: |-
                The date and time of the earliest shipment being manifested. Formatted per the
                https://tools.ietf.org/html/rfc3339 spec
              format: date-time
              nullable: true
            close_datetime:
              type: string
              description: |-
                The date and time of the last shipment being manifested. Formatted per the
                https://tools.ietf.org/html/rfc3339 spec
              format: date-time
            advanced_options:
              description: |-
                This is a schemaless object. This is a join of all of the advanced options for all of the shipments being
                manifested. It is for open ended customizations unique to particular carriers. The documented keys are some
                common options shared by many carriers, but are not definitive. Advanced options you support will be defined
                in ShipEngine. If the field is absent it should be interpreted as the default value for any applicable
                options, e.g. false for booleans.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/AdvancedOptions'
    CreateLabelResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: This model represents the response from a successful create label request.
          additionalProperties: false
          properties:
            transaction_id:
              type: string
              description: The transaction ID uniquely represents this request.
              format: guid
            label_download:
              description: |-
                This object provides the requested label that was just created. Exactly one of the keys should be
                populated. Set href if the label should be downloaded separately from a public URL. Set data if the
                label is returned directly in the response.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/LabelDownload'
            form_download:
              description: |-
                This is deprecated, use documents array with type = customs_form
                This provides any additional forms that go with the label. For example, customs forms. Exactly one of the
                keys should be populated. Set href is the forms should be downloaded separately. Set data if the
                forms are returned directly in the response.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/FormDownload'
            documents:
              type: array
              description: |-
                Shipment level documents should be used if documents are not returned at a package level.
                DO NOT include duplicate documents intentionally, a document should only be added to the CreateLabelResponse
                or to the LabelPackage.
              nullable: true
              items:
                $ref: '#/components/schemas/Document'
            packages:
              type: array
              description: |-
                Package information returned for each requested package in the shipment.
                Note: The order of the packages returned should match the order of the packages in the request.
              nullable: true
              items:
                $ref: '#/components/schemas/LabelPackage'
            billing_line_items:
              type: array
              description: Individual Billing Line items for the label.
              nullable: true
              items:
                $ref: '#/components/schemas/BillingLineItem'
            tracking_number:
              type: string
              description: |-
                The carrier specific tracking identifier for this shipment; if a multi-package shipment, this should be
                the lead tracking number.
              nullable: true
            trackable:
              type: boolean
              description: Whether this shipment can be tracked.
              nullable: true
            alternative_identifiers:
              type: array
              description: Alternative identifiers associated with this shipment.
              nullable: true
              items:
                $ref: '#/components/schemas/Identifier'
            estimated_delivery_datetime:
              type: string
              description: |-
                The estimated date and time for when the shipment will be delivered. Formatted per the
                https://tools.ietf.org/html/rfc3339 spec.
              format: date-time
              nullable: true
    LabelDownload:
      allOf:
        - $ref: '#/components/schemas/ResourceDownload'
        - type: object
          description: |-
            This object provides the requested label that was just created. Exactly one of the keys should be populated.
            Set href if the label should be downloaded separately. Set data if the label is returned directly in
            the response.
          additionalProperties: false
          properties:
            label_data:
              type: string
              description: The base64 encoded label image.
              nullable: true
    FormDownload:
      allOf:
        - $ref: '#/components/schemas/ResourceDownload'
        - type: object
          description: |-
            This provides any additional forms that go with the label. For example, customs forms. Exactly one of the
            keys should be populated. Set href is the forms should be downloaded separately. Set data if the
            forms are returned directly in the response.
          additionalProperties: false
          properties:
            form_data:
              type: string
              nullable: true
    Document:
      type: object
      description: Container for a document.
      additionalProperties: false
      properties:
        type:
          type: array
          description: |-
            The document type(s) being returned. In most cases this is just a single document type; however, some
            carriers return multiple document types in the same PDF or ZPL.
          nullable: true
          items:
            $ref: '#/components/schemas/DocumentType'
        data:
          type: string
          description: Base64 encoded data for the document.
          nullable: true
        format:
          description: Format of the document.
          oneOf:
            - $ref: '#/components/schemas/DocumentFormat'
    DocumentType:
      type: string
      description: ''
      x-enumNames:
        - Label
        - CustomsForm
        - CommercialInvoice
        - BillOfLading
      enum:
        - label
        - customs_form
        - commercial_invoice
        - bill_of_lading
    DocumentFormat:
      type: string
      description: ''
      x-enumNames:
        - Pdf
        - Zpl
        - Png
      enum:
        - PDF
        - ZPL
        - PNG
    LabelPackage:
      type: object
      description: Package information provided from the carrier
      additionalProperties: false
      properties:
        tracking_number:
          type: string
          description: Tracking number for the package.
          nullable: true
        alternative_identifiers:
          type: array
          description: 'Alternative identifiers associated with this package, not including the tracking number.'
          nullable: true
          items:
            $ref: '#/components/schemas/Identifier'
        documents:
          type: array
          description: |-
            Documents associated with this package, if documents are not specified at a package level, use the
            Documents array at the CreateLabelResponse level.
          nullable: true
          items:
            $ref: '#/components/schemas/Document'
    CreateLabelRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          additionalProperties: false
          properties:
            service_code:
              type: string
              description: |-
                The service code uniquely identifies a shipping service that you offer. Which service codes can be passed
                to you will be configured in ShipEngine.
              nullable: true
            ship_datetime:
              type: string
              description: |-
                When the package is expected to ship. Not guaranteed to be in the future. Formatted per the
                https://tools.ietf.org/html/rfc3339 spec. Will always be in UTC.
              format: date-time
            confirmation:
              description: The expected delivery confirmation. If the field is absent it should be interpreted as "None"
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ConfirmationTypes'
            label_format:
              description: The expected format of the returned label.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/DocumentFormat'
            label_layout:
              description: The expected layout of the returned label.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/LabelLayouts'
            is_test_label:
              type: boolean
              description: |-
                Whether this request is the result of a test. When true the request must not result in any financial charges
                to any party. If the field is absent it should be interpreted as false
            advanced_options:
              description: |-
                This is a schemaless object. It is for open ended customizations unique to particular carriers. The
                documented keys are some common options shared by many carriers, but are not definitive. Advanced options
                you support will be defined in ShipEngine. If the field is absent it should be interpreted as the default
                value for any applicable options, e.g. false for booleans.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/AdvancedOptions'
            insurance_provider:
              description: |-
                The insurance provider for the insured value of the label, `carrier` indicates that the user is requesting
                insurance from the carrier directly, anything else is extra information that should not result
                in a transaction.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/InsuranceProviders'
            is_return_label:
              type: boolean
              description: |-
                Whether this label is to return a previously shipped package. If the field is absent it should be
                interpreted as false.
            is_residential:
              type: boolean
              description: |-
                Whether this package ships from a residential address. If the field is absent it should be
                interpreted as false
            packages:
              type: array
              description: All the packages that make up this shipment. There will always be at least one package defined.
              nullable: true
              items:
                $ref: '#/components/schemas/Package'
            customs:
              description: |-
                This describes the customs declarations of this shipment. This field should be present
                for international shipments.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/Customs'
            ship_to:
              description: The shipment recipient's address. It may or may not have been validated.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipTo'
            ship_from:
              description: The shipment sender's address. It may or may not have been validated.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipFrom'
            pickup_location:
              description: |-
                If this is a pickup shipment, a pickup location will be populated for where the recipient can
                pickup the shipment.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/PudoLocation'
            ship_from_display:
              description: 'The address that should be displayed as the return address, only if the carrier supports this functionality'
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipFromDisplay'
            next_day:
              type: boolean
              description: |-
                Whether this shipment is expected to use a next day service class. If the field is absent it
                should be interpreted as false
              nullable: true
            international:
              type: boolean
              description: Whether this shipment is deemed international.
              nullable: true
            reference:
              type: string
              description: A user specified free form string to identify this shipment in their own system.
              nullable: true
            shipment_details:
              type: object
              description: This property has been deprecated. Please use fulfillment_plan_details.
              nullable: true
              additionalProperties: {}
            return_details:
              description: 'If the label is for a return, return details should be specified. (Added 1.13)'
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ReturnLabelDetails'
            fulfillment_plan_details:
              description: 'Details regarding the fulfillment plan and/or original sales order, only if the carrier provider requires this info for label generation'
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/FulfillmentPlanDetails'
            attachments:
              type: array
              description: |-
                Attachments that may be required to be sent to the carrier, in particular customs documents or
                commercial invoices.
              nullable: true
              items:
                $ref: '#/components/schemas/Document'
    LabelLayouts:
      type: string
      description: ''
      x-enumNames:
        - FourBySix
        - Letter
      enum:
        - 4x6
        - letter
    ShipFromDisplay:
      allOf:
        - $ref: '#/components/schemas/AddressBase'
        - type: object
          description: |-
            The address that should be displayed as the return address, only if the shipping provider supports
            this functionality.
          additionalProperties: false
    ReturnLabelDetails:
      type: object
      description: Return Label Information (Added 1.13)
      additionalProperties: false
      properties:
        rma_number:
          type: string
          description: |-
            A return merchandise authorization (RMA) is an associated number assigned to process the return, this
            number is often printed on the label, and used when the original shipper processes the inbound return.
          nullable: true
    VoidLabelsResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - type: object
          description: This model represents the successful attempt to void a set of labels.
          additionalProperties: false
          properties:
            void_responses:
              type: array
              description: Responses to void a particular label.
              nullable: true
              items:
                $ref: '#/components/schemas/VoidResponse'
    VoidResponse:
      type: object
      description: This model represents the success or failure of an individual label being voided.
      additionalProperties: false
      properties:
        void_request_id:
          type: string
          description: This unique identifier should be related to the void_request_id in the VoidLabelsRequest.
          format: guid
        message:
          type: string
          description: User facing text describing the void process. May be left blank to present a default value.
          nullable: true
        errors:
          type: array
          description: |-
            A list of errors preventing this label from being voided. If no errors are returned the void is considered
            successful. If any errors are returned the void is considered failed.
          nullable: true
          items:
            type: string
    VoidLabelsRequest:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          description: This model represents the payload sent to void a set of labels.
          additionalProperties: false
          properties:
            void_requests:
              type: array
              description: Requests to void labels.
              nullable: true
              items:
                $ref: '#/components/schemas/VoidRequest'
    VoidRequest:
      allOf:
        - $ref: '#/components/schemas/ShippedShipment'
        - type: object
          description: A request to void a particular label.
          additionalProperties: false
          properties:
            void_request_id:
              type: string
              description: Uniquely identifies this request to void a label. Must be returned in a void response.
              format: guid
            ship_from:
              description: The shipment sender's address. It may or may not have been validated.
              nullable: true
              oneOf:
                - $ref: '#/components/schemas/ShipFrom'
    ExampleRegisterResponse:
      allOf:
        - $ref: '#/components/schemas/RegisterResponseOfDictionaryOfStringAndString'
        - type: object
          additionalProperties: false
    RegisterResponseOfDictionaryOfStringAndString:
      type: object
      additionalProperties: false
      properties:
        credentials:
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/Credentials'
        metadata:
          type: object
          nullable: true
          additionalProperties:
            type: string
    Credentials:
      type: object
      description: |-
        The credentials that will be used to access this carrier's services. These will be sent as a
        Basic Authorization header with all future requests.
      additionalProperties: false
      properties:
        username:
          type: string
          nullable: true
        password:
          type: string
          nullable: true
    ExampleRegisterRequest:
      allOf:
        - $ref: '#/components/schemas/RegisterRequestOfDictionaryOfStringAndString'
        - type: object
          additionalProperties: false
    RegisterRequestOfDictionaryOfStringAndString:
      allOf:
        - $ref: '#/components/schemas/BaseRequest'
        - type: object
          additionalProperties: false
          properties:
            registration_info:
              type: object
              nullable: true
              additionalProperties:
                type: string
